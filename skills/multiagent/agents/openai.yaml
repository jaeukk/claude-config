interface:
  display_name: "Multiagent"
  short_description: "Route Claude and Codex roles under validated task contracts"
  default_prompt: "Use $multiagent to conduct this task with model-independent roles and scoped worker contracts."
policy:
  allow_implicit_invocation: true
