interface:
  display_name: "Conductor"
  short_description: "Route Claude and Codex roles under validated task contracts"
  default_prompt: "Use $conductor to conduct this task with model-independent roles and scoped worker contracts."
policy:
  allow_implicit_invocation: true
